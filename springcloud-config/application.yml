server:
  port: 81
spring:
  profiles:
    active: test
---
spring:
  application:
    name: moyingren-config-dev
  profiles: dev
---
spring:
  application:
    name: moyingren-config-test
  profiles: test
